Mettre ma libft qui compile avant le projet via un seul make
	la checker avec claude

comprendre
	◦mmap(2)
	◦munmap(2)
	◦getpagesize under OSX or sysconf(_SC_PAGESIZE) under linux
	◦getrlimit(2)
	◦Any other system call (syscall)
	◦The functions from libpthread

But :
	#include <stdlib.h>
	void free(void *ptr);
	void *malloc(size_t size);
	void *realloc(void *ptr, size_t size)